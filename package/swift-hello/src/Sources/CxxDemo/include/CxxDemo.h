#pragma once

#include <string>

namespace demo {

/// Adds two numbers in C++.
int add(int lhs, int rhs);

/// Returns a greeting built with std::string to exercise libstdc++.
std::string greeting(const std::string &name);

} // namespace demo
